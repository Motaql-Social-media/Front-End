import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // Customize your theme options here (e.g., palette, typography)
  palette: {
    primary: {
      main: "#007bff",
    },
    secondary: {
      main: "#198754",
    },
  },
  typography: {
    fontFamily: "Roboto",
  },
});

export default theme;
